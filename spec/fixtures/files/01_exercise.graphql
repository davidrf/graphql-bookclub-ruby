query exercise_01($id: ID!, $orderBy: UserConnectionOrderInput) {
  repository(id: $id) {
    collaboratorsConnection(orderBy: $orderBy) {
      edges {
        node {
          id
        }
      }
    }
  }
}
